# 阶段 1：直接使用 Nginx 作为基础镜像
FROM nginx:1.23-alpine

# 复制小程序源码到 Nginx 的静态资源目录
COPY miniprogram/ /usr/share/nginx/html

# 可选：覆盖 Nginx 默认配置（如需调整跨域、缓存策略等）
# COPY miniprogram/nginx.conf /etc/nginx/conf.d/default.conf

# 暴露 80 端口（与 docker-compose 映射）
EXPOSE 80